services:
  watchtower:
    image: containrrr/watchtower:latest
    container_name: watchtower
    env_file:
      - .env
    command: --cleanup --interval ${WATCHTOWER_POLL_INTERVAL:-86400}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
